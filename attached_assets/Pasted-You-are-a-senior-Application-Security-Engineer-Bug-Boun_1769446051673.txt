You are a senior Application Security Engineer, Bug Bounty Hunter, and Static Analysis Architect.

Context:
Our current JavaScript Security Analyzer is giving false positive result and need to be detect leaked secrets, sensitive identifiers, and exploitable security issues in JavaScript files.

The analyzer must prioritize real-world exploitability and minimize false positives.

implement this in fast and pro mode and in pro mode need to performt this on subdomains also.

Goal:
Surface only high-confidence, real security leaks that would be accepted in bug bounty programs, while suppressing cosmetic or theoretical findings.

────────────────────────────────────
PRIMARY HIGH-VALUE LEAK TYPES
────────────────────────────────────

1. Cloud & Infrastructure Secrets (CRITICAL)
   - AWS Access Keys (AKIA / ASIA)
   - AWS Secret Access Keys
   - GCP API Keys
   - Azure Storage Keys & Connection Strings
   - Firebase Admin SDK private keys
   - Signed Cloud URLs (AWS S3, GCS, Azure Blob)

2. API Keys, Access Tokens & Auth Secrets (CRITICAL / HIGH)
   - API keys (validated by entropy, length, prefix)
   - Bearer tokens
   - OAuth access tokens & refresh tokens
   - JWT tokens (header.payload.signature format)
   - Session tokens
   - Service-to-service authentication tokens
   - WebSocket authentication tokens

3. Developer, Platform & CI/CD Secrets
   - GitHub / GitLab / Bitbucket access tokens
   - GitHub Actions, GitLab CI, Jenkins tokens
   - NPM, Docker, PyPI authentication tokens
   - Slack, Discord, Telegram, Microsoft Teams webhooks
   - Feature flag secrets (LaunchDarkly, Split.io)

4. Database & Backend Credentials
   - MongoDB connection strings
   - PostgreSQL / MySQL credentials
   - Redis, Elasticsearch, RabbitMQ URLs
   - Supabase, PlanetScale, Neon DB credentials

5. Payment & Financial Secrets (CRITICAL)
   - Stripe live secret keys (exclude test keys)
   - Razorpay live keys
   - PayPal client secrets
   - Payment gateway webhook secrets

6. Cryptographic Material (CRITICAL)
   - RSA / EC private keys
   - OpenSSH private keys
   - PEM / PFX private certificates
   - JWT signing secrets

7. Personally Identifiable Information (PII – STRICT)
   - Email addresses (RFC-compliant, real domains only)
   - Valid mobile numbers (E.164 format)
   - Aadhaar / PAN / SSN (opt-in only)
   - Usernames tied to auth flows

8. Identifiers With Security Impact (BUG BOUNTY RELEVANT)
   - User IDs used in API endpoints
   - UUIDs / GUIDs used as resource identifiers
   - Account IDs, organization IDs, tenant IDs
   - Session IDs
   - Order IDs, transaction IDs
   - Reset tokens, verification tokens

   IMPORTANT:
   Flag these ONLY when:
   - They appear hardcoded or exposed in client-side JS
   - They are used in API calls, auth flows, or object access
   - They could lead to IDOR, account takeover, or data exposure

9. Mobile & Frontend Service Secrets
   - Firebase config secrets
   - OneSignal API keys
   - Expo, Vercel, Netlify access tokens
   - Analytics write keys (Segment, Mixpanel)

10. Misconfiguration & Bug Bounty Gold
   - Hardcoded admin/debug endpoints
   - Feature flags exposing internal functionality
   - Environment variables leaked to frontend
   - Internal service URLs
   - Source map (.map) references exposing source code

────────────────────────────────────
FALSE POSITIVE REDUCTION RULES
(MUST ENFORCE)
────────────────────────────────────

1. Third-Party & Vendor Code
   - Classify assets.*, cdn.*, static.*, vendor.*, node_modules
     and known vendor domains as THIRD-PARTY
   - Suppress findings unless user-controlled data is involved

2. Entropy & Validation
   - Ignore low-entropy, placeholder, mock, or example values
   - Enforce entropy thresholds per secret type

3. Context Awareness
   - Only flag values that are:
     - Hardcoded
     - Reachable
     - Used in runtime logic
   - Ignore comments, docs, test, demo code

4. Pattern + Validator Model
   - Never rely on regex alone
   - Combine regex + prefix + length + entropy + context

5. Dangerous JS APIs
   - eval(), document.write(), innerHTML:
     - Flag ONLY if user input flows into the sink
     - Else classify as INFO or CODE_SMELL

6. IP Address Handling
   - Treat IPs as sensitive ONLY if tied to user data
   - Ignore CDN, vendor, infrastructure IPs

────────────────────────────────────
OUTPUT REQUIREMENTS
────────────────────────────────────

- Only high-confidence leaks should be CRITICAL or HIGH
- Suppressed findings must include clear explanation
- Confidence scoring must reflect exploitability
- Optimize for bug bounty acceptance standards
- Maintain scan performance

Think like a real attacker and optimize for real-world impact.

if you found any duplicate code then remove it 